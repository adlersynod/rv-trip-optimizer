[railway]
version = 2

[build]
builder = "NIXPACKS"

[nixpacks]
pythonVersion = "3.11"

[deploy]
startCommand = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
healthCheckPath = "/"
